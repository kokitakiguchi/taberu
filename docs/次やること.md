# 次やること

最終更新：2026-05-18

---

## 今どこまで動いているか

- [x] 写真をアップロードする画面がある
- [x] バックエンドAPIが動く
- [x] DBに保存される
- [x] 一覧表示される
- [ ] **Claude Vision で本当にAI分析できるか未確認**（←ここが次のヤマ）

---

## 既存タスク（変わらず優先）

### 1. Claude APIキーをセットして分析を動かす ← 最優先

`.env` ファイルに以下を書いてバックエンドを再起動する：

```
CLAUDE_API_KEY=sk-ant-xxxxxxxx
```

確認方法：写真をアップロードして、カロリーや食材名が自動で入力されたら成功。

---

### 2. 分析が失敗したときの動作を確認する

AI分析に失敗したとき「手動で入力してください」という画面が出るか確認する。
（APIキーなしで試せば再現できる）

---

### 3. 統計グラフを確認する

Analytics ページ（`/analytics`）を開いて、グラフが実際のデータで描画されるかみる。
グラフが出ない場合はバックエンドの `/api/stats/calories` を確認。

---

### 4. 大きい画像を送ってみる

5MB を超える写真を送ったとき、自動でリサイズされてエラーにならないか確認する。
（iPhoneの写真などは大きいことが多い）

---

## 完了済みタスク（参考）

### entry_mode 対応（2026-05-16〜）

- [x] `migrations/002_add_entry_mode.sql` 追加（DB 適用は backend 起動時に自動）
- [x] バックエンド：4 モード分岐 (`dish_photo` / `nutrition_label` / `text_ai` / `text_manual`)
- [x] フロントエンド：EntryModeSelector / TextEntryForm / ManualNutritionForm / AllergenEditor

### 本番デプロイ整備（2026-05-18）

- [x] backend 起動時に `sqlx::migrate!` で migrations 自動適用
- [x] 本番手順を `.env` 編集 → `docker compose up` の 2 ステップに短縮
- [x] README / docs/design.md の重複・古い手順を整理

---

## この先（MVP完成後にやること）

- MCP（アレルゲン辞書など）を試す
- 食品成分データベースとの連携
- スマホからも使いやすいようにUIを整える
- 記録の全フィールド編集（料理名・食材・カロリー・PFC）

---

## 困ったとき

- 設計の詳細 → `README.md`
- Claudeへの指示ルール → `.claude/rules/`
